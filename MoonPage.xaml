<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Moonstone3.MoonPage"
             BackgroundColor="Black"
             Title="MoonPage">
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">


          
            <Label 
            x:Name="label"
            Text="Moonstone"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

            <!--<Switch
                x:Name="moonstone"
                
                HorizontalOptions="Center"
                IsToggled="False"
                Toggled="switcher_Toggled"
                />-->
            <ImageButton
                x:Name="moonpic"
                Source="moon2.png"
                SemanticProperties.Description="moon"
                HeightRequest="200"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Clicked="OnImageButtonClicked"
                
               />
            <!--<Button
                x:Name="SndBtn"
                Text="Sound Board"
                Clicked="OnSndBtnClicked"
                
                />-->

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>